apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: felipegoes7/user_service:latest
        ports:
        - containerPort: 5003
        env:
        - name: DB_NAME
          value: user_db
        - name: DB_USER
          value: admin
        - name: DB_PASSWORD
          value: admin123
        - name: DB_HOST
          value: postgres-users
        - name: DB_PORT
          value: "5432"
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 5003
      targetPort: 5003
  type: LoadBalancer
